version: 0.0
Resources:
  - TargetService:
      Type: AWS::EKS::Service
      Properties:
        ClusterName: brain-tasks-cluster
        Namespace: default
        TaskDefinition: k8s/deployment.yaml

